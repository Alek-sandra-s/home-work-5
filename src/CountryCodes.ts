export function generateCodeEstonia() {
  const randomNumber = Math.floor((Math.random() + 1) * 1000)
  const estoniaPrefix = 'EST-'

  return estoniaPrefix + randomNumber
}

export function generateCodeLatvia() {
  const randomNumber = Math.floor((Math.random() + 1) * 1000)
  const latviaPrefix = 'LVA-'

  return latviaPrefix + randomNumber
}

export function generateCodeLithuania() {
  const randomNumber = Math.floor((Math.random() + 1) * 1000)
  const LithuaniaPrefix = 'LTU-'

  return LithuaniaPrefix + randomNumber
}
